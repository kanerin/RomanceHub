<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8">
    <title>Sample_GoogleMap</title>

    <script src="http://maps.google.com/maps/api/js?key=<%= ENV['API_KEY'] %>&language=ja"></script>
    <%= stylesheet_link_tag 'post_lists', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'post_lists', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div id=info_area>
      <div id=form_area>
          <%= form_for @post, url: post_lists_path do |f| %>
          <div class="field">
              <%= f.label :title %>
              <%= f.text_field :title %>
          </div>
        
          <div class="field">
              <%= f.label :content %>
              <%= f.text_area :content %>
          </div>
        
          <div class="actions">
            <%= f.submit "投稿" %>
          </div>
        <% end %>
      </div>
      <div id=post_area>
          <ul>
        <% @posts.each do |post| %>
            <li>
            <h2><%= post.title %></h2>
            <p><%= post.content %></p>
            </li>
        <% end %>
        </ul>
      </div>
    </div>
    <>
    <div id=map_area>
      <div id="map">
      </div>
    </div>

  </body>
</html>
